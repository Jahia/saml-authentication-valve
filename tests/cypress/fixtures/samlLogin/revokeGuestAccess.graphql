mutation revokeGuestAccess($nodePath: String!) {
    jcr {
        mutateNode(pathOrId: $nodePath) {
            revokeRoles(
                principalName: "guest"
                principalType: USER
                roleNames: ["reader"]
            )
        }
    }
}

