---
# Allowed version bumps: patch, minor, major
saml-authentication-valve: patch
---

Fixed the SAML login button so a title that contains special characters now renders as written. The form it submits now carries the page name the same way.
